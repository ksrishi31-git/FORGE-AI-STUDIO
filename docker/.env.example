# ForgeAI Studio — Docker Compose environment template
# Copy to docker/.env before running `docker compose up`.

# PostgreSQL
POSTGRES_USER=forgeai
POSTGRES_PASSWORD=forgeai
POSTGRES_DB=forgeai

# Backend (override as needed)
APP_ENV=development
LOG_LEVEL=INFO
CORS_ORIGINS=["http://localhost:3000"]
