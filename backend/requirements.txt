# --- Core web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
email-validator>=2.2,<3.0

# --- Authentication ---
PyJWT>=2.10,<3.0
passlib[bcrypt]>=1.7,<2.0
# bcrypt pinned below 4.1: passlib 1.7.4 is incompatible with bcrypt >= 4.1
bcrypt>=4.0,<4.1

# --- Data layer ---
sqlalchemy[asyncio]>=2.0.36,<2.1
asyncpg>=0.30,<1.0
alembic>=1.14,<2.0

# --- Infrastructure clients ---
redis>=5.2,<7.0

# --- Observability ---
loguru>=0.7,<1.0

# --- Agent orchestration (AI layer; consumed from Phase 3.2+) ---
langgraph>=0.2,<1.0
langchain-core>=0.3,<1.0
langchain-openai>=0.2,<1.0
openai>=1.50,<2.0

# --- Vector memory (consumed from Phase 3.2+) ---
chromadb>=0.5,<2.0

# --- HTTP client ---
httpx>=0.27,<1.0
