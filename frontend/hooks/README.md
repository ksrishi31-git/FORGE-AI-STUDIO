# Hooks

Application hooks (FAD §4) — TanStack Query hooks wrapping the typed services,
WebSocket subscription hooks, and UI hooks.

## Rules

1. Hooks are the only client-side consumers of `services/`.
2. Query hooks follow the key hierarchy documented in FAD §4.2.

This directory is intentionally empty in Phase 3.1 (project foundation).
